# Data — our parquets + HINT benchmark (symlink) + materialized canonical
# datasets. Large / immutable / regenerable; never committed.
data/

# Pulled-in HINT subtree: code is tracked, big artifacts are not.
hint/.venv/
hint/data/*.pkl
hint/data/*.csv
hint/save_model/*.ckpt
hint/results/
hint/figure/

# Run outputs.
runs/

# Python / uv.
.venv/
__pycache__/
*.pyc
.pytest_cache/
uv.lock
